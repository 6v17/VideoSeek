# VideoSeek — CUDA experiment branch dependencies
# Preferred install:
#   pip install -r requirements.txt
#   pip install rapidocr-onnxruntime==1.4.4 --no-deps
#
# Do NOT install onnxruntime / onnxruntime-directml on this branch.
# If `python -c "import onnxruntime as ort; print(ort.get_available_providers())"`
# lacks CUDAExecutionProvider, run:
#   pip uninstall -y onnxruntime
#   pip install --force-reinstall --no-deps onnxruntime-gpu

onnxruntime-gpu
PyNvVideoCodec
nvidia-cuda-runtime-cu12
cupy-cuda12x

opencv-python==4.13.0.92
scenedetect>=0.6.4,<0.8
PySide6==6.10.2
faiss-cpu==1.13.2
lancedb==0.34.0
numpy==2.2.6
pillow==12.1.1
pillow-heif==1.3.0
# SigLIP ONNX profiles load tokenizer.json via Hugging Face tokenizers (default clip_onnx does not need this)
tokenizers==0.22.2
ftfy
regex
yt-dlp
rookiepy
python-vlc

# RapidOCR deps listed explicitly; install the package itself with --no-deps (see header).
pyclipper>=1.2.0
Shapely!=2.0.4,>=1.7.1
PyYAML
six
tqdm
# rapidocr-onnxruntime==1.4.4  # install via: pip install rapidocr-onnxruntime==1.4.4 --no-deps

# Mobile bridge (local HTTP server / phone upload) — see src/web/mobile_bridge.py
fastapi==0.136.0
uvicorn==0.44.0
python-multipart

# QR code for mobile bridge dialog — optional; UI works without it but QR image may be missing
qrcode[pil]

# Dev / CI
pytest>=9.0
ruff>=0.11
