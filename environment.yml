name: VideoSeek-CUDA
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytest>=9.0
  - pip:
      - -r requirements.txt
# After create: pip uninstall -y onnxruntime
# (rapidocr may pull CPU ORT; keep onnxruntime-gpu only)
